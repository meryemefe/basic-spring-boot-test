# Basic Spring Boot Test Application

This repository contains a simple Spring Boot application used for testing purposes.

## Testing

Tests are implemented in the `src/test` directory and can be run with Maven.

## Test by Agent

An AI agent can be used to modify and verify project files. This commit updates the README and demonstrates a "test by agent" change.

